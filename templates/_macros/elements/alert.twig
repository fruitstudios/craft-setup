{% set alertTitle = alertTitle ?? false %}
{% set alertSummary = alertSummary ?? false %}

<div class="bg-green-lightest border-l-4 border-green text-green-dark p-4 mb-4" role="alert">
    <p class="font-bold">{{ alertTitle }}</p>
    <p>{{ alertSummary }}</p>
</div>

